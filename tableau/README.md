# tableau/

Tableau 대시보드 파일과 게시 링크 보관 위치. 비식별 연결 데이터와 설계 문서는 완성했으며 `.twbx`와 Tableau Public URL은 아직 없다.

`make tableau-data`는 `tableau/data/`에 5개 연결 CSV를 다시 만든다. `dashboard_spec.md`는 숫자 카드 대신 매출 집중·코호트·거래 활동·쿠폰 관찰 차이의 관계를 보여주는 차트로 구성한다.
